An end-to-end ELT data pipeline that ingests job posting data from external REST APIs, stores raw JSON payloads in PostgreSQL, and transforms them into structured relational tables for analytical querying.

This project demonstrates practical data engineering concepts including API ingestion, JSON normalization, relational modeling, and staged data transformation.
